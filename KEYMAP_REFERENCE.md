# roBa.keymap 構造・コード詳細解説リファレンス

このドキュメントは、`config/roBa.keymap` の内部構造、各マクロ、および各レイヤーの挙動をエンジニア視点で詳細に解読・記録したリファレンスです。

---

## ❓ なぜコード上で「右手と左手」に分かれているのか？

ZMKでは、左右のマイコン（XIAO BLE）に載っている物理スイッチを、1つの大きな統合マトリクス・テーブルとして合体させて管理しています。
`roBa.dtsi` 内の `default_transform`（座席変換テーブル）の処理によって、コード上の「左側5個が左手」「右側5個が右手」というように物理ピンへ自動マッピングされるため、人間が視覚的にメンテしやすいようにスペースを開けて記述されています。

---

## 📜 ソースコード 1行（ブロック）ずつの詳細解説

### 1. ライブラリ読み込み部
```dts
#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/pointing.h>
```
ZMKの基本動作、Bluetooth制御、標準キーコード、およびトラックボール（ポインティングデバイス）操作用の標準ライブラリをシステムにインクルードしています。

### 2. モディファイア・タップ（&mt）の最適化
```dts
&mt {
    flavor = "balanced";
    quick-tap-ms = <0>;
};
```
タップで文字、ホールドでレイヤー/Shiftになるキー（`&mt`）の判定をチューニングし、高速タイピング時の誤作動（ロールオーバー誤判定）を徹底的に防止する設定です。

### 3. トラックボール（PMW3610）のオートレイヤー制御
```dts
&trackball {
    automouse-layer = <4>;
    scroll-layers = <5>;
};
```
* `automouse-layer = <4>`: トラックボールを1ピクセルでも動かすと、自動的にキーマップが『レイヤー4（MOUSE）』に切り替わるオートマウス機能。
* `scroll-layers = <5>`: レイヤー5（SCROLL）滞在時に球を転がすと、カーソル移動ではなく「画面の上下スクロール」に機能をすり替える。

### 4. コンボ（同時押し）設定
```dts
combos {
    compatible = "zmk,combos";
    tab { bindings = <&kp TAB>; key-positions = <11 12>; };
    ...
};
```
特定の2つの物理キーを完全に同時にカチッと叩いた際、単体キー（TAB、Shift+Tab、無変換、ダブルクォーテーション、イコール等）を独立して発動させる入力レイヤーのショートカットマッピングです。

### 5. マクロおよび独自ビヘイビア定義
```dts
macros { ... to_layer_0 ... };
behaviors { ... lt_to_layer_0 ... };
```
「強制的にレイヤー0に戻り、かつ日本語入力切り替えキーを送信する」という動的マクロや、それをホールド・タップ機能と融合させたこのキーボード専用の「新型カスタムボタン」を自作してシステムに登録しています（長押し判定：200ms）。

---

## 🗺️ 各レイヤー（キー座席表）の役割

* **レイヤー0 (default_layer)**: 普段の文字入力（QWERTY）。Macの画面スクショマクロ（Cmd+Shift+4）や、親指長押しでの各レイヤーワープ、Iキー長押しでのスクロール変身が仕込まれています。
* **レイヤー1 (FUNCTION)**: F1〜F12を制御する画面。不必要な場所は `&trans`（下のレイヤーを透過）に設定されています。
* **レイヤー2 (NUM)**: スペース長押しで出現。右手側が電卓のような「数字テンキー」へと化け、数字の高速入力が可能です。
* **レイヤー3 (ARROW)**: 左手側に上・下・左・右の矢印やブラウザのタブ切り替えを集中配置した編集作業用画面。
* **レイヤー4 (MOUSE)**: トラックボールに触れた瞬間に自動起動。右手にマウスの左クリック（MB1）、中クリック（MB3）、右クリック（MB2）が配置されます。
* **レイヤー5 (SCROLL)**: Iキー長押しで発動。この画面内での球の回転はすべて「画面スクロール（ホイール）」にすり替わります。
* **レイヤー6 (layer_6)**: Bluetoothの端末切り替え（BT_SEL）や、現在のペアリング記憶を消去して強制的に新しい待ち受けモードにする初期化コマンド（BT_CLR）が配置されたシステム管理画面。
