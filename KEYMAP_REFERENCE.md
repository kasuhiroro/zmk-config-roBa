# roBa.keymap 構造・コード詳細解説リファレンス

このドキュメントは、`config/roBa.keymap` の内部構造、各マクロ、および各レイヤーの挙動を詳細に解読・記録したリファレンスです。

---

## ❓ なぜコード上で「右手と左手」に分かれているのか？

ZMK（およびこのroBa）では、「左手側」と「右手側」の物理スイッチを、1つの大きな統合マトリクス・テーブルとして合体させて管理しています。

1. **マイコンは別々でも、脳みそは1つ**：
   実物のキーボードは物理的に左右に分割されていて、それぞれにXIAO BLE（マイコン）が載っています。しかし、ファームウェア（ZMK）をコンパイルするときは、「左右が合体して1つの大きなキーボード（キーマップ）」になるように設計図を作ります。
2. **座席表（マトリクス・トランスフォーム）のルール**：
   `boards/shields/roBa/roBa.dtsi` 内の `default_transform` という座席変換テーブルにより、「コードの上半分の左側5個は左手の1行目」「右側の5個は右手の1行目」という風に、物理ピンとキーマップの記述が自動的に紐づけられます。

人間がコードを読んだときに「ここが左手の人差し指だな」「ここが右手の薬指だな」と直感的にメンテナンスしやすいように、スペースを空けて配置されています。

---

## 📜 ソースコード ブロックごとの徹底解説

### 1. ヘッダー・ライブラリ読み込み部
```dts
#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/pointing.h>
```
C言語のインターフェース・ヘッダーのインクルードと同じです。ZMKの基本動作（`behaviors`）、Bluetooth制御用のコマンド（`bt.h`）、一般的なキーコード（`keys.h`）、そしてトラックボールなどのマウス操作コマンド（`pointing.h`）の標準ライブラリをシステムにインクルードしています。

### 2. モディファイア・タップ（&mt）のデフォルト挙動変更
```dts
&mt {
    flavor = "balanced";
    quick-tap-ms = <0>;
};
```
「タップしたら文字、ホールドしたらShiftやCtrlになるキー（`&mt`）」の判定アルゴリズムをチューニングしています。
* `flavor = "balanced";` : タイピングが速い人が「文字を打っただけなのにShiftと判定されてしまう誤作動」を防ぐ、ZMKで最もバランスが良いとされる判定モード。
* `quick-tap-ms = <0>;` : 同じキーを連打したときに、ホールド（Shift）にならず必ず「文字の連打」として認識させる設定。

### 3. トラックボール（PMW3610）のオートレイヤー設定
```dts
&trackball {
    automouse-layer = <4>;
    scroll-layers = <5>;
};
```
roBa（およびZMKのポインティング拡張）の強力な制御コアです。
* `automouse-layer = <4>;` : トラックボールの球に触れて1ピクセルでも動かすと、自動的にキーマップが『レイヤー4（MOUSE）』に切り替わるオートマウス機能。球から手を離すと自動で元のレイヤーに戻る。
* `scroll-layers = <5>;` : 特定の条件（レイヤー5）にいるときは、トラックボールの球を転がすとカーソル移動ではなく「画面のスクロール（マウスホイール）」の動きに変化させる。

### 4. コンボ（同時押し）設定
```dts
combos {
    compatible = "zmk,combos";

    tab {
        bindings = <&kp TAB>;
        key-positions = <11 12>;
    };

    shift_tab {
        bindings = <&kp LS(TAB)>;
        key-positions = <12 13>;
    };

    muhennkann {
        bindings = <&to_layer_0 INT_MUHENKAN>;
        key-positions = <11 10>;
    };

    double_quotation {
        bindings = <&kp DOUBLE_QUOTES>;
        key-positions = <20 21>;
    };

    eq {
        bindings = <&kp EQUAL>;
        key-positions = <24 25>;
    };
};
```
「2つのキーを完全に同時に押したときに、別のキーに変身させる」というコンボ機能を定義しています。
* `tab` : キーボード全体の「11番目」と「12番目」の物理ボタン（左手の特定の2つ）を同時に押すと `TAB` キーが発動。
* その他の定義（`shift_tab`, `muhennkann`, `double_quotation`, `eq`）も同様に、特定の2箇所を同時押しした際に、Shift+Tab、無変換、ダブルクォーテーション、イコール等を発動させる独自のショートカットマッピング。

### 5. マクロ（一連のキー操作の自動化）定義
```dts
macros {
    to_layer_0: to_layer_0 {
        compatible = "zmk,behavior-macro-one-param";
        #binding-cells = <1>;
        bindings = <&to 0 &macro_param_1to1 &kp MACRO_PLACEHOLDER>;
        label = "TO_LAYER_0";
    };
};
```
引数（パラメータ）を1つ受け取って実行する、ZMKの高度な動的マクロです。
「まず強制的に基本のレイヤー0（`&to 0`）に戻り、その直後に引数として渡された日本語入力切り替えキー（無変換など）をPCに送信する」という2つの連続した挙動を、1つのボタンにまとめて実行させる関数のような仕組み。

### 6. 拡張ビヘイビア（挙動）の自作
```dts
behaviors {
    lt_to_layer_0: lt_to_layer_0 {
        compatible = "zmk,behavior-hold-tap";
        label = "LAYER_TAP_TO_0";
        bindings = <&mo>, <&to_layer_0>;

        #binding-cells = <2>;
        tapping-term-ms = <200>;
    };
};
```
ZMK標準の「ホールドタップ（`&lt`）」をベースに、上記のオリジナルマクロを合体させた、このキーボード専用の「新型のカスタムボタン」を自作して登録しています。
`tapping-term-ms = <200>;` により、「長押し」と判定するまでの猶予時間を「200ミリ秒」に指定。200ミリ秒以上押しっぱなしにすると、レイヤー切り替えの機能が発動。

---

## 🗺️ 各レイヤー（キー座席表）の役割と詳細挙動

### 🟦 レイヤー0：`default_layer`（普段の文字入力）
パソコンを起動したときに最初に有効になっている、ベースのQWERTY配列です。
* **マクロボタン**: 左手の真ん中にある `&kp LS(LG(S))` は、Macの画面スクショ（Cmd + Shift + 4）を親指一発で起動します。
* **レイヤー5連動**: 右手の「I」の場所にある `&lt 5 I` は、普通に押すと「I」ですが、押しっぱなしにすると「レイヤー5（SCROLL）」に切り替わり、トラックボールがスクロールホイールに変身します。
* **レイヤー2連動**: 最下段の `&lt 2 SPACE` は、スペースキーを長押しすると、レイヤー2（数字入力画面）へワープします。

### 🟦 レイヤー1：`FUNCTION`（ファンクションキー画面）
F1〜F12を入力するための画面です。
* `&trans` (Transparent) : 「透明なキー」という意味。このレイヤーに切り替わっている間、ここを押すと1つ下のレイヤー（普段の画面）のキー（QやWなど）がそのまま透過して発動するパススルー設定。

### 🟦 レイヤー2：`NUM`（数字テンキー ＆ 記号画面）
スペースキー（`&lt 2 SPACE`）を長押ししている間だけ出現する画面です。右手の中心部分が、電卓のような「数字テンキー（`KP_NUMBER_7`など）」の配置に化けるため、数字の高速入力ができるようになっています。

### 🟦 レイヤー3：`ARROW`（矢印・カーソル移動画面）
左手側に、上・下・左・右の矢印キー（`UP_ARROW` など）や、ブラウザのタブ切り替え（`LC(TAB)`）を集中配置した、編集作業用の画面です。

### 🟦 レイヤー4：`MOUSE`（マウスボタン画面）
トラックボールの球に触れた瞬間に、自動で裏側で発動する画面です。
右手の中心に、マウスの左クリック（`MB1`）、中クリック（`MB3`）、右クリック（`MB2`）がマッピングされており、球を転がしながら右手でそのままクリックが可能です。

### 🟦 レイヤー5：`SCROLL`（画面スクロール画面）
「I」のキーを長押ししている間だけ発動する画面です。キーの配置はすべて透明（`&trans`）ですが、冒ラー定義（`scroll-layers = <5>;`）のルールにより、この画面にいる間だけはトラックボールを転がすと上下の「画面スクロール」に機能がすり替わります。

### 🟦 レイヤー6：`layer_6`（Bluetooth・システム制御画面）
パソコンやスマホなど、複数の通信相手を切り替えるためのシステム管理画面です。
* `&bt BT_SEL 0` : 1台目のデバイス（MacBookなど）に接続を切り替え。
* `&bt BT_CLR` : 分割キーボードのペアリングに関する問題を解決するための設定初期化コマンド。現在のペアリング記憶を消去して強制的に新しい待ち受けモードにします（普段の「/」キーの場所に配置）。
